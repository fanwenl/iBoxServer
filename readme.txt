服务器端需求：
1、能够识别设备端的MAC和IMEI号，作为设备联网，传输数据的唯一的凭证。
	认证失败断掉连接。
2、在设备匹配的是手动输出MAC和IMEI，存入数据库中(网页)。
3、使用WIFI传输时验证MAC.GPRS时采用IMEI。
4、数据传输采用JSON格式。
5、传输协议使用MQTT。
7、设备侧能够获取服务器的时间。
8、设备能够上报一些信息给服务器存储。
9、服务器端设置一个简单的页面，显示实时设备侧上传的信息，可以选择设备。
	对于温度等一些数据，以曲线的方式显示。其他数据以具体的数值显示(预留数值显示的接口)
10、设置简单的输入框，作为下行传输测试。
11、使用python作为主要语言，使用自带的数据库。12、不涉及手机APP的部分。

python -m pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple/


/*可以参考IBOX历程中的做法*/